apiVersion: batch/v1
kind: Job
metadata:
  name: migration-safe-expand
  namespace: opsproof
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: psql
          image: postgres:18-alpine
          env:
            - name: PGHOST
              value: postgres
            - name: PGDATABASE
              value: opsproof
            - name: PGUSER
              value: postgres
          command: ["psql", "-v", "ON_ERROR_STOP=1", "-f", "/migrations/safe-expand.sql"]
          volumeMounts:
            - name: migrations
              mountPath: /migrations
      volumes:
        - name: migrations
          configMap:
            name: migration-safe-expand
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: migration-safe-expand
  namespace: opsproof
data:
  safe-expand.sql: |
    ALTER TABLE orders ADD COLUMN IF NOT EXISTS buyer_name TEXT;

