apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: opsproof-observability
generatorOptions:
  disableNameSuffixHash: true
  labels:
    grafana_dashboard: "1"
configMapGenerator:
  - name: opsproof-schema-compatibility-dashboard
    files:
      - schema-compatibility.json
